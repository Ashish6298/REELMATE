import 'package:flutter/material.dart';

class InstagramPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Instagram Page")),
      body: Center(
        child: Text("Welcome to Instagram"),
      ),
    );
  }
}
